@using Bookish.Models
@model CheckoutCatalogueViewModel

<div class="text-center">
    <h1 class="display-4">Account history for: @Model.CheckoutCatalogue[0].Member.FirstName @Model.CheckoutCatalogue[0].Member.Surname </h1>
    <table class="table table-striped table-bordered"
        <tr> 
            <th></th>
            <th>Copy Id</th>
            <th>Book Title</th>
            <th>Still Checked Out? </th>
            <th>Checkout Date</th>
            <th>Due Date</th>
            <th>Returned?</th>
        </tr>
    @foreach (var checkout in Model.CheckoutCatalogue) 
    {
        <tr>
            <td> @checkout.CheckoutId </td>
            <td> @checkout.Copy.CopyId</td>
            <td> @checkout.Copy.Book.Title</td>
            <td> @checkout.Copy.CheckedOut</td>
            <td> @checkout.CheckoutDate </td>
            <td> @checkout.DueDate</td>
            <td> @checkout.Returned</td>
        </tr>
    }
    </table>
</div>
    @* <table class="table table-striped table-bordered">
        <tr> 
            <th>Copy Id</th>
            <th>Checked out?</th>
            <th></th>
        </tr>
    @foreach (var copy in Model.CopyCatalogue) 
    {
        <tr>
            <td> @copy.CopyId </td>
            <td> @copy.CheckedOut </td>
            <td>
                @Html.ActionLink("Checkout History", "Checkout", new { id=copy.Book.BookId }) |
                @Html.ActionLink("Delete Copy", "DeleteCopy", new { id=copy.Book.BookId }) 
            </td>
        </tr>
    }
    </table>
    @Html.ActionLink("Add a New Copy", "AddCopy", new { id = Model.CopyCatalogue[0].Book.BookId }) <br />
    @Html.ActionLink("Back to Catalogue", "Index") 
</div> *@

